{
  "name": "recipe_server",
  "version": "1.0.0",
  "description": "Backend server for recipe app",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "start_legacy": "nodemon -L index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}